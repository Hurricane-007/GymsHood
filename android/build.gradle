
// buildscript {
//         repositories {
//         google()  // ✅ Required to resolve Google services
//         mavenCentral()  // ✅ Optional but useful
//     }
//         dependencies {
//         classpath 'com.android.tools.build:gradle:8.2.2' // Use your existing version
//         classpath 'com.google.gms:google-services:4.4.2' // ✅ This is what it's trying to resolve
//     }

// }
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
// subprojects {
//     afterEvaluate { project ->
//         if (project.hasProperty('android')) {
//             project.android {
//                 if (namespace == null) {
//                     namespace project.group
//                 }
//             }
//         }
//     }
// }
subprojects {
    project.evaluationDependsOn(':app')
}
rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
